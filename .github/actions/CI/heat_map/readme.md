## 测试 Graph 接口

代码见 [test.ts](./test.ts).

读取导出的 node_data/index.json, 创建热力图.
其中, 基类节点用深色表示, 派生类节点用浅色表示; 不同颜色的方块对应不同类型的节点, 白色为不存在的节点(或者是 Struct 的泛类).

图中每行有 100 个节点(从0开始), 共37行, 对应 3669 个可能的节点.

*生成效果:*
![heat_map](./heat_map.jpg)