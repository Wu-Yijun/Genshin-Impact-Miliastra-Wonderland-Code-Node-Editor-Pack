关于“隐藏节点 (Type 5)”的究竟是什么? 我不太能提出猜测. 全部已知信息如下:
节点有两种: 纯运算节点, 包含控制流的执行节点.
从数据来看, 有且仅有子系统B的执行节点有一个该类型的引脚(且仅存在一个), 且两个次序编号均为0. 其结构与(类型为 int 且值为 0 的)数据输入引脚结构一致. 且该引脚不显示, 值始终为空. 该引脚数据中还有一个额外的字段, 内部值与节点的第一个id相同.
特例1:有少量隐藏节点(系统中不能直接从图标获取, 可以通过导入修改后的文件在系统中显示), 它们不包含这一字段.
特例2. 重要: 有一个节点是 Send Signal, 需要提供


// Quick Note: Signal 特有的, 从 Server 的 Signal 中导出也会出现的!!!